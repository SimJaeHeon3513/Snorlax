#include <stdio.h>

int main()
{
    int N;
    int honeycomb = 1, count = 1;
    scanf("%d", &N);
    
    while(N > honeycomb){
        if(N == 1){
            break;
        }
        honeycomb += 6 * count;
        count++;
    }
    printf("%d", count);
    return 0;
}
